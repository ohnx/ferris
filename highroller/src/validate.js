// validate all of the edges
const jsep = require("jsep");

// fsm has same format, so does opts
exports.validate = function(fsm, opts) {
    
};

exports.parseEdge = function(edgeStr) {
    return jsep(edgeStr);
};
